
name: 'deploy'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel previous runs
        uses: styfle/cancel-workflow-action@0.4.0
        with:
          access_token: ${{ github.token }}

      - name: Check out code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get identifier for PR
        run: |
          echo "TAG=$(echo ${{ github.ref_name }} | sha1sum | cut -c -10)" >> $GITHUB_ENV

      - name: Rename tag to :latest and remove
        run: |
          docker buildx imagetools create camer0/nico:${{ env.TAG }} --tag camer0/nico:latest

          TOKEN=$(curl -s -H "Content-Type: application/json" -X POST -d \
            '{"username": "${{ secrets.DOCKERHUB_USERNAME }}", "password": "${{ secrets.DOCKERHUB_TOKEN }}"}' \
            https://hub.docker.com/v2/users/login/ | jq -r .token)

          curl -i -X DELETE \
            -H "Accept: application/json" \
            -H "Authorization: JWT $TOKEN" \
            https://hub.docker.com/v2/repositories/camer0/nico/tags/${{ env.TAG }}/

      # - name: Push to dokku
      #   uses: dokku/github-action@master
      #   with:
      #     git_remote_url: ${{ secrets.DOKKU_SSH_URL }}
      #     ssh_private_key: ${{ secrets.DOKKU_SSH_PK}}
